{
  "name": "@dnr-ui/tokens",
  "version": "0.0.3",
  "devDependencies": {
    "@divriots/style-dictionary-to-figma": "^0.4.0",
    "style-dictionary": "^3.7.1",
    "token-transformer": "^0.0.27"
  },
  "description": "Design Tokens to be used in DNR applications",
  "main": "index.js",
  "files": [
    "build"
  ],
  "scripts": {
    "clean": "style-dictionary clean",
    "transform": "npm run transform:core && npm run transform:semantic && npm run transform:components",
    "transform:core": "npx token-transformer figma/tokens.json data/core.json core --expandTypography=true",
    "transform:semantic": "npx token-transformer figma/tokens.json data/semantic.json core,semantic core --expandTypography=true --resolveReferences=true",
    "transform:components": "npx token-transformer figma/tokens.json data/components.json core,components core --expandTypography=true --resolveReferences=true",
    "build:style-dictionary": "style-dictionary build",
    "build": "npm run transform && npm run build:style-dictionary",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "publishConfig": {
    "registry": "https://artifactory.persgroep.cloud/artifactory/api/npm/npm-local",
    "access": "restricted"
  },
  "license": "MIT"
}
